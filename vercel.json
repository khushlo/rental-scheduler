{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "npm install && npx prisma generate && npx prisma migrate deploy",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}